import React from 'react';
import AppBar from 'material-ui/AppBar';

export const MyAppBar = ({title}) => (
  <AppBar
    title={title}
    iconClassNameRight="muidocs-icon-navigation-expand-more"/>
);
